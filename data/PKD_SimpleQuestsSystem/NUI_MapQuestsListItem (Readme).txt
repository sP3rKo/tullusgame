"activeQuestAppendText" - Text to append to the quest name if it is active. %1 will be replaced with the quest name.

"autoRefreshIntervalInSeconds" - How often the quest text should be refreshed in seconds. 0 to disable.
If you use control characters, like \V, in quest task text, you should set this to > 0.